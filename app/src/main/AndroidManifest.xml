<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.danielakinola.loljournal">

    <application
        android:name=".LolJournalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/LolJournalTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".champpool.ChampPoolActivity"
            android:label="@string/title_activity_champ_pool" />
        <activity android:name=".championselect.ChampionSelectActivity" />
        <activity
            android:name=".matchups.MatchupsActivity"
            android:label="@string/title_activity_matchups"
            android:parentActivityName=".champpool.ChampPoolActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.danielakinola.loljournal.champpool.ChampPoolActivity" />
        </activity>
        <activity android:name=".matchupdetail.MatchupDetailActivity" />
        <activity android:name=".editcomment.EditCommentActivity" />
        <activity android:name=".commentdetail.CommentDetailActivity" />
    </application>

</manifest>

    <!--TODO: setup parent activities-->